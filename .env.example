# Example environment configuration for VixfloStream Downloader
# Copy to .env and adjust as needed.

# If you keep FFmpeg elsewhere, set AVE_FFMPEG_PATH to either:
# - the folder containing ffmpeg.exe/ffprobe.exe (e.g. C:\path\to\bin)
# - OR directly to ffmpeg.exe
#
# By default, the app also auto-detects FFmpeg in:
#   ./tools/ffmpeg/bin  (downloaded via tools\ffmpeg\ensure_ffmpeg.bat)
#   ./ffmpeg/bin        (if present locally)
#   PATH
#
# AVE_FFMPEG_PATH=

# Optional: cookies file for restricted videos
# AVE_COOKIES_FILE=C:\path\to\cookies.txt

# Optional: server binding (used by launcher scripts)
# AVE_HOST=127.0.0.1
# AVE_PORT=8000

# Optional: URL to open automatically in browser-mode launcher
# AVE_OPEN_URL=http://127.0.0.1:8000/
