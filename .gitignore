.venv/
downloads/
__pycache__/
*.pyc

# Local environment / secrets
.env
.env.*
!.env.example

# Safety: never commit git recovery backups
.git_corrupt_*/

# Runtime logs
logs/
*.log

# Build artifacts / installers (often >100MB)
dist/
build/
desktop-electron/dist/

# Node dependencies
desktop-electron/node_modules/

# Bundled ffmpeg distribution (large; keep out of source repo)
ffmpeg/

# Downloaded ffmpeg binaries (kept out of git, but bundled into EXEs at build time)
tools/ffmpeg/bin/
tools/ffmpeg/_cache/
tools/ffmpeg/_extract/
tools/ffmpeg/*.zip
tools/ffmpeg/*.7z
